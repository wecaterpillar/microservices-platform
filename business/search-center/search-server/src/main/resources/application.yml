spring:
  elasticsearch:
    rest:
      uris: ${ea.elasticsearch.uris}
      username: ${ea.elasticsearch.username}
      password: ${ea.elasticsearch.password}

ea:
  indices:
    show: sys-log-*,mysql-slowlog-*,sys_user,point-log-*,audit-log-*
  swagger:
    enabled: true
    title: 搜索中心
    description: 搜索中心接口文档
    version: 1.0
    base-package: org.openea.search.controller
